#!/usr/bin/env perl
use v5.14.1;
use lib 'lib';
use GBV::App::Covers;
my $app = GBV::App::Covers->new("covers.json");
use File::Find;
find(sub {
    next if -d $_;
    say join ' ', $app->lookup_file($_);
}, $app->{cache});

